-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.actors (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  name text NOT NULL,
  photo_url text,
  birth_date date,
  nationality text,
  biography text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  death_date date,
  gender text,
  tmdb_id text,
  imdb_id text,
  role USER-DEFINED DEFAULT 'actor'::person_role_type,
  is_main_person boolean DEFAULT true,
  known_for_department text,
  popularity numeric DEFAULT 0,
  place_of_birth text,
  also_known_as ARRAY,
  CONSTRAINT actors_pkey PRIMARY KEY (id),
  CONSTRAINT actors_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.collection_items (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  collection_id uuid NOT NULL,
  content_id uuid,
  series_id uuid,
  podcast_id uuid,
  position integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT collection_items_pkey PRIMARY KEY (id),
  CONSTRAINT collection_items_collection_id_fkey FOREIGN KEY (collection_id) REFERENCES public.collections(id),
  CONSTRAINT collection_items_content_id_fkey FOREIGN KEY (content_id) REFERENCES public.content(id),
  CONSTRAINT collection_items_series_id_fkey FOREIGN KEY (series_id) REFERENCES public.series(id),
  CONSTRAINT collection_items_podcast_id_fkey FOREIGN KEY (podcast_id) REFERENCES public.podcasts(id)
);
CREATE TABLE public.collections (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  name text,
  description text,
  cover_image text,
  collection_type text DEFAULT 'custom'::text CHECK (collection_type = ANY (ARRAY['custom'::text, 'franchise'::text, 'universe'::text, 'anthology'::text])),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT collections_pkey PRIMARY KEY (id),
  CONSTRAINT collections_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.content (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  name text,
  cover_image text,
  rating numeric CHECK (rating >= 0::numeric AND rating <= 10::numeric),
  duration integer,
  series_id uuid,
  season integer,
  episode integer,
  watched_date date,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  release_year integer,
  watched_year integer,
  watched_month integer CHECK (watched_month >= 1 AND watched_month <= 12),
  watched_day integer CHECK (watched_day >= 1 AND watched_day <= 31),
  date_unknown boolean DEFAULT false,
  review text,
  type USER-DEFINED NOT NULL DEFAULT 'other'::content_type,
  season_id uuid,
  episode_number integer,
  would_recommend boolean,
  would_rewatch boolean,
  watch_status USER-DEFINED DEFAULT 'completed'::watch_status_type,
  rewatch_count integer DEFAULT 0,
  last_rewatch_date date,
  content_subtype text,
  CONSTRAINT content_pkey PRIMARY KEY (id),
  CONSTRAINT content_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT content_series_id_fkey FOREIGN KEY (series_id) REFERENCES public.series(id)
);
CREATE TABLE public.content_actors (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  content_id uuid,
  series_id uuid,
  actor_id uuid NOT NULL,
  original_role_name text,
  created_at timestamp with time zone DEFAULT now(),
  character_name text,
  is_main_cast boolean DEFAULT false,
  credit_order integer,
  episode_count integer,
  CONSTRAINT content_actors_pkey PRIMARY KEY (id),
  CONSTRAINT content_actors_actor_id_fkey FOREIGN KEY (actor_id) REFERENCES public.actors(id),
  CONSTRAINT content_actors_content_id_fkey FOREIGN KEY (content_id) REFERENCES public.content(id),
  CONSTRAINT content_actors_series_id_fkey FOREIGN KEY (series_id) REFERENCES public.series(id)
);
CREATE TABLE public.content_crew (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  content_id uuid NOT NULL,
  person_id uuid NOT NULL,
  role USER-DEFINED NOT NULL,
  job_title text,
  department text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT content_crew_pkey PRIMARY KEY (id),
  CONSTRAINT content_crew_content_id_fkey FOREIGN KEY (content_id) REFERENCES public.content(id),
  CONSTRAINT content_crew_person_id_fkey FOREIGN KEY (person_id) REFERENCES public.actors(id)
);
CREATE TABLE public.content_genres (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  content_id uuid NOT NULL,
  genre_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT content_genres_pkey PRIMARY KEY (id),
  CONSTRAINT content_genres_content_id_fkey FOREIGN KEY (content_id) REFERENCES public.content(id),
  CONSTRAINT content_genres_genre_id_fkey FOREIGN KEY (genre_id) REFERENCES public.genres(id)
);
CREATE TABLE public.content_viewings (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  content_id uuid NOT NULL,
  watched_date date,
  watched_year integer,
  watched_month integer,
  date_precision text DEFAULT 'full'::text CHECK (date_precision = ANY (ARRAY['full'::text, 'month'::text, 'year'::text])),
  date_unknown boolean DEFAULT false,
  rating numeric CHECK (rating >= 0::numeric AND rating <= 10::numeric),
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  episode_id uuid,
  podcast_episode_id uuid,
  watch_session_id uuid,
  platform text,
  device text,
  duration_watched integer,
  CONSTRAINT content_viewings_pkey PRIMARY KEY (id),
  CONSTRAINT content_viewings_content_id_fkey FOREIGN KEY (content_id) REFERENCES public.content(id),
  CONSTRAINT content_viewings_episode_id_fkey FOREIGN KEY (episode_id) REFERENCES public.series_episodes(id),
  CONSTRAINT content_viewings_podcast_episode_id_fkey FOREIGN KEY (podcast_episode_id) REFERENCES public.podcast_episodes(id),
  CONSTRAINT content_viewings_watch_session_id_fkey FOREIGN KEY (watch_session_id) REFERENCES public.watch_sessions(id)
);
CREATE TABLE public.data_transfers (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  transfer_type text NOT NULL CHECK (transfer_type = ANY (ARRAY['import'::text, 'export'::text])),
  file_name text,
  file_size_bytes integer,
  content_count integer DEFAULT 0,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'processing'::text, 'completed'::text, 'failed'::text])),
  error_message text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  completed_at timestamp with time zone,
  CONSTRAINT data_transfers_pkey PRIMARY KEY (id),
  CONSTRAINT data_transfers_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.genres (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  name text NOT NULL,
  description text,
  is_custom boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT genres_pkey PRIMARY KEY (id),
  CONSTRAINT genres_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.list_items (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  list_id uuid NOT NULL,
  content_id uuid,
  series_id uuid,
  position integer DEFAULT 0,
  added_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  notes text,
  CONSTRAINT list_items_pkey PRIMARY KEY (id),
  CONSTRAINT list_items_list_id_fkey FOREIGN KEY (list_id) REFERENCES public.lists(id),
  CONSTRAINT list_items_content_id_fkey FOREIGN KEY (content_id) REFERENCES public.content(id),
  CONSTRAINT list_items_series_id_fkey FOREIGN KEY (series_id) REFERENCES public.series(id)
);
CREATE TABLE public.lists (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  name text NOT NULL,
  description text,
  cover_image text,
  is_public boolean DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT lists_pkey PRIMARY KEY (id),
  CONSTRAINT lists_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.podcast_episodes (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  podcast_id uuid NOT NULL,
  name text,
  episode_number integer,
  season integer DEFAULT 1,
  cover_image text,
  rating numeric CHECK (rating >= 0::numeric AND rating <= 10::numeric),
  duration integer,
  watched_date date,
  watched_year integer,
  watched_month integer,
  date_precision text DEFAULT 'full'::text CHECK (date_precision = ANY (ARRAY['full'::text, 'month'::text, 'year'::text])),
  date_unknown boolean DEFAULT false,
  notes text,
  review text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  would_recommend boolean,
  would_rewatch boolean,
  rewatch_count integer DEFAULT 0,
  last_rewatch_date date,
  guest_hosts ARRAY,
  episode_url text,
  transcript text,
  CONSTRAINT podcast_episodes_pkey PRIMARY KEY (id),
  CONSTRAINT podcast_episodes_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT podcast_episodes_podcast_id_fkey FOREIGN KEY (podcast_id) REFERENCES public.podcasts(id)
);
CREATE TABLE public.podcast_hosts (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  podcast_id uuid NOT NULL,
  person_id uuid NOT NULL,
  role text DEFAULT 'host'::text,
  start_date date,
  end_date date,
  episode_count integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT podcast_hosts_pkey PRIMARY KEY (id),
  CONSTRAINT podcast_hosts_podcast_id_fkey FOREIGN KEY (podcast_id) REFERENCES public.podcasts(id),
  CONSTRAINT podcast_hosts_person_id_fkey FOREIGN KEY (person_id) REFERENCES public.actors(id)
);
CREATE TABLE public.podcasts (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  name text,
  cover_image text,
  status text DEFAULT 'in_progress'::text CHECK (status = ANY (ARRAY['in_progress'::text, 'abandoned'::text, 'completed'::text])),
  release_year integer,
  description text,
  host text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  poster_vertical text,
  poster_horizontal text,
  would_recommend boolean,
  would_rewatch boolean,
  average_rating numeric,
  total_listen_time integer DEFAULT 0,
  start_date date,
  end_date date,
  language text,
  website_url text,
  rss_feed text,
  CONSTRAINT podcasts_pkey PRIMARY KEY (id),
  CONSTRAINT podcasts_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  display_name text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  avatar_url text,
  bio text,
  location text,
  website_url text,
  preferences jsonb DEFAULT '{}'::jsonb,
  total_watch_time integer DEFAULT 0,
  total_content_count integer DEFAULT 0,
  last_export_date timestamp with time zone,
  last_import_date timestamp with time zone,
  theme_preference text DEFAULT 'light'::text,
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.series (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  name text,
  cover_image text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  release_year integer,
  status text DEFAULT 'in_progress'::text CHECK (status = ANY (ARRAY['in_progress'::text, 'abandoned'::text, 'completed'::text])),
  total_seasons integer DEFAULT 0,
  total_episodes integer DEFAULT 0,
  poster_vertical text,
  poster_horizontal text,
  would_recommend boolean,
  would_rewatch boolean,
  average_rating numeric,
  total_watch_time integer DEFAULT 0,
  has_special_seasons boolean DEFAULT false,
  start_date date,
  end_date date,
  description text,
  CONSTRAINT series_pkey PRIMARY KEY (id),
  CONSTRAINT series_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.series_cast (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  series_id uuid NOT NULL,
  actor_id uuid NOT NULL,
  character_name text NOT NULL,
  is_main_cast boolean DEFAULT true,
  episode_count integer,
  season_range text,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT series_cast_pkey PRIMARY KEY (id),
  CONSTRAINT series_cast_series_id_fkey FOREIGN KEY (series_id) REFERENCES public.series(id),
  CONSTRAINT series_cast_actor_id_fkey FOREIGN KEY (actor_id) REFERENCES public.actors(id)
);
CREATE TABLE public.series_episode_structure (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  series_id uuid NOT NULL,
  season integer NOT NULL,
  episode integer NOT NULL,
  name text,
  duration integer,
  is_watched boolean DEFAULT false,
  release_date date,
  description text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT series_episode_structure_pkey PRIMARY KEY (id),
  CONSTRAINT series_episode_structure_series_id_fkey FOREIGN KEY (series_id) REFERENCES public.series(id)
);
CREATE TABLE public.series_episodes (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  series_id uuid NOT NULL,
  season_id uuid NOT NULL,
  episode_number integer NOT NULL,
  name text,
  duration integer,
  is_watched boolean DEFAULT false,
  content_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  would_recommend boolean,
  would_rewatch boolean,
  rewatch_count integer DEFAULT 0,
  last_rewatch_date date,
  release_date date,
  rating numeric CHECK (rating >= 0::numeric AND rating <= 10::numeric),
  review text,
  CONSTRAINT series_episodes_pkey PRIMARY KEY (id),
  CONSTRAINT series_episodes_series_id_fkey FOREIGN KEY (series_id) REFERENCES public.series(id),
  CONSTRAINT series_episodes_content_id_fkey FOREIGN KEY (content_id) REFERENCES public.content(id)
);
CREATE TABLE public.series_seasons (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  series_id uuid NOT NULL,
  user_id uuid NOT NULL,
  season_number integer NOT NULL,
  name text,
  episode_count integer DEFAULT 0,
  watched_episode_count integer DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  is_special boolean DEFAULT false,
  special_type text,
  poster_vertical text,
  poster_horizontal text,
  release_year integer,
  average_rating numeric,
  total_watch_time integer DEFAULT 0,
  CONSTRAINT series_seasons_pkey PRIMARY KEY (id),
  CONSTRAINT series_seasons_series_id_fkey FOREIGN KEY (series_id) REFERENCES public.series(id),
  CONSTRAINT series_seasons_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.user_goals (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  goal_type text NOT NULL,
  target_value integer NOT NULL,
  current_value integer DEFAULT 0,
  start_date date NOT NULL,
  end_date date NOT NULL,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'completed'::text, 'failed'::text, 'abandoned'::text])),
  title text NOT NULL,
  description text,
  reward text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_goals_pkey PRIMARY KEY (id),
  CONSTRAINT user_goals_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.user_statistics (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  stat_date date NOT NULL DEFAULT CURRENT_DATE,
  stat_type text NOT NULL,
  movies_watched integer DEFAULT 0,
  series_watched integer DEFAULT 0,
  episodes_watched integer DEFAULT 0,
  shorts_watched integer DEFAULT 0,
  podcasts_listened integer DEFAULT 0,
  other_watched integer DEFAULT 0,
  total_watch_time integer DEFAULT 0,
  average_daily_watch_time integer DEFAULT 0,
  average_rating numeric DEFAULT 0,
  highest_rated_content_id uuid,
  lowest_rated_content_id uuid,
  top_genres jsonb DEFAULT '[]'::jsonb,
  rewatch_count integer DEFAULT 0,
  recommended_count integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_statistics_pkey PRIMARY KEY (id),
  CONSTRAINT user_statistics_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.watch_sessions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone,
  duration_minutes integer,
  platform text,
  device text,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT watch_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT watch_sessions_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);